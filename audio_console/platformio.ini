[platformio]
build_dir = ../build/audio_console
default_envs = audio_console

[env]
framework = arduino
monitor_speed = 115200
; Console UART via Picoprobe (GP0/GP1). Pico W USB CDC is typically the other cu.usbmodem*.
monitor_port = /dev/cu.usbmodem11102
upload_protocol = cmsis-dap
monitor_flags = --echo
; CMSIS-DAP (Picoprobe) is usually auto-detected; set upload_port if you have multiple probes.
platform = https://github.com/maxgerhardt/platform-raspberrypi.git
board = rpipicow
board_build.core = earlephilhower
build_flags = -DEDGE_AUDIO_CONSOLE

lib_deps =
	https://github.com/pschatzmann/arduino-audio-tools.git
	https://github.com/adafruit/Adafruit_TLV320_I2S.git
lib_extra_dirs =
	../audio/lib
	../shared_libs
lib_ignore = sound_bank, sd_test, bluetooth, audio

[env:audio_console]
debug_tool = cmsis-dap
build_type = debug
debug_build_flags = -O0 -ggdb3 -g3
